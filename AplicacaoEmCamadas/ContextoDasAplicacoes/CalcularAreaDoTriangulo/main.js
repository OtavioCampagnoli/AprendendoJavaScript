import CalcularAreaDoTringulo from "./Calculos.js";

let base = 5;
let altura = 10;

let area01 = CalcularAreaDoTringulo(base, altura);

console.log(`A área do triângulo com base: ${base} e altura: ${altura} é: ${area01} `);