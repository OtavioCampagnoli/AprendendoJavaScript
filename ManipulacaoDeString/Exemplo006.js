/*
    Substituir substring dentro de determinada string;
    Replace("String que está no texto", "Aquilo que quero substituir");

    Sendo assim, o método replace() é a melhor forma de substituir um termo dentro de um texto na linguagem JavaScript;

*/


let text = "Testando a linguagem Java."

let trocando = text.replace("Java", "JavaScript");

console.log(trocando);